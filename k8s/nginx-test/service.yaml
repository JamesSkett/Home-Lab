apiVersion: v1
kind: Service
metadata:
  name: pi-web-test-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: pi-web-test
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30083
